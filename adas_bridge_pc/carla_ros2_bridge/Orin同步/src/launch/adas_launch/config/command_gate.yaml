# 命令门控（速度断点须严格递增——启动期审计，非法直接拒绝启动）
# /**: 命名空间无关（双栈冗余下 /primary /backup 同用此文件）
/**:
  ros__parameters:
    rate_hz: 50.0
    follower_timeout_s: 0.2
    stop_decel_mps2: 2.5
    steer_decay_tau_s: 1.0
    filter:
      speed_points_mps: [0.0, 10.0, 20.0, 30.0]
      steer_lim_rad: [0.6, 0.35, 0.2, 0.12]
      steer_rate_lim_rps: [0.8, 0.5, 0.35, 0.25]
      max_accel_mps2: 3.0
      max_decel_mps2: 8.0
      max_jerk_mps3: 10.0
