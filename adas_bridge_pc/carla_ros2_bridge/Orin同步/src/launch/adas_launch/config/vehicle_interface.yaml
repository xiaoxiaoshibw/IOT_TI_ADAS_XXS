# 车辆接口（标定值须与 sim_vehicle.yaml 的 vehicle.* 一致）
/**:
  ros__parameters:
    rate_hz: 50.0
    watchdog_timeout_s: 0.2
    watchdog_brake: 1.0
    watchdog_steer_decay_tau_s: 0.5
    adapter: sim
    sim:
      max_accel_mps2: 3.0
      max_decel_mps2: 8.0
      max_steer_rad: 0.6
